# nothing to do here ;) - the method returns a full name with names capitalized (notice it inside interpolation)
def build_name(first, middle, last)
  return "#{first.capitalize} #{middle.capitalize} #{last.capitalize}"
end
